# 수의 합을 저장할 변수 sum을 만든다.
sum = 0

# 1부터 100까지 반복한다.
for (i in 1:100) {
  # 만약 i가 3으로 나누었을때 나머지가 0이고, 4로 나누었을때 나머지가 0이 아니라면,
  if (i %% 3 == 0 && i %% 4 != 0) {
    # 조건이 제대로 입력되었는지 확인하기 위해 i를 출력한다.
    print(i)
    # sum변수에 현재 i값을 더해준다.
    sum = sum + i
  }
}

# sum을 출력한다.
print(sum)
